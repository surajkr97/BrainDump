export default function AppFooter() {
  return (
    <footer className="mt-auto flex flex-wrap items-baseline justify-between gap-x-4 gap-y-1 border-t border-line pt-5 text-[14px] text-muted">
      <span>Dump it here, deal with it later.</span>
      <span className="font-mono text-[13px]">Next.js · Django REST</span>
    </footer>
  );
}
